Plug 'simeji/winresizer'

" If you want to start window resize mode by `Ctrl+R`
let g:winresizer_start_key = '<C-R>'
" If you want to cancel and quit window resize mode by `esc` (keycode 27)
let g:winresizer_keycode_cancel = 27

